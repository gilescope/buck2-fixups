# jni's build script (cfg probe) is present in the 0.22.0..<0.23.0 versions
# the dated snapshots resolve, but gone/absent in the main rig's version.
# Gate so reindeer wires it only where it exists (a version cfg matching no
# resolved version is exempt). Surfaced by the snapshot rigs; see issue #45.
['cfg(version = ">=0.22.0, <0.23.0")']
buildscript.run = false
