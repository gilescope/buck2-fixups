# lock_api 0.4.0-0.4.12 ships a build.rs (a rustc 1.61 version probe); 0.4.13+
# dropped it and 0.3.x never had it. Version-gate to that exact band so the
# directive isn't "unused" against the other lock_api versions the rigs resolve.
['cfg(version = ">=0.4.0, <0.4.13")']
buildscript.run = false
